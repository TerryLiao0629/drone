pipeline:
  build:
    image: plugins/ecr
    region: ap-northeast-1
    tag: ${DRONE_COMMIT}
    repo: nice_drone
    secrets: [ ecr_access_key, ecr_secret_key ]
    dockerfile: Dockerfile
    when:
      branch: develop
